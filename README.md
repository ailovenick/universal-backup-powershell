# 🛡️ Universal PowerShell Backup

**Универсальный скрипт для резервного копирования файлов и папок с поддержкой архивации (ZIP) и ротации старых копий.**

### ℹ️ О проекте
Изначально этот инструмент разрабатывался для **резервного копирования файловых баз 1С:Предприятие в небольшой сети "Work group"**. Позже скрипт был переработан в **универсальное решение**.

Теперь он подходит для бэкапа чего угодно: от баз данных (`.1CD`, `.mdf`) до рабочих документов,любых папок с файлами.

---

### 🚀 Возможности
*   **Два режима:**
    *   📂 **Копирование:** Создает точную копию папки (удобно для быстрого доступа).
    *   📦 **Архивация:** Упаковывает данные в ZIP (экономит место).
*   **Умная ротация:** Хранит заданное количество последних копий (например, 7), старые удаляет автоматически.
*   **Логирование:** Ведет подробный лог-файл (если не указать конкретно, то имя лога совпадает с именем скрипта).
*   **Простота:** Не требует установки стороннего ПО (работает на чистой Windows).

---

### ⚙️ Настройка
Откройте файл скрипта (`.ps1`) в любом текстовом редакторе и измените блок настроек в начале файла:

| Параметр | Описание |
| :--- | :--- |
| `$SourcePath` | Путь к тому, **ЧТО** копировать (файл или папка). |
| `$DestDir` | Путь, **КУДА** сохранять копии. |
| `$EnableZip` | `$true` — делать ZIP-архив, `$false` — просто копировать папку. |
| `$MaxCopies` | Количество копий, которые нужно хранить (ротация). |
| `$LogFile` | Пустое значение создаёт лог около скрипта namescript.log или прописать вручную. |


---

### 📅 Автозапуск (Планировщик заданий)

Чтобы настроить автоматический бэкап (например, каждую ночь):

1.  Нажмите `Win + R`, введите **`taskschd.msc`** и нажмите Enter.
2.  Нажмите **"Создать задачу..."** (не простую, а обычную).
3.  **Вкладка "Общие":**
    *   Имя: `Backup`
    *   Отметьте: **"Выполнять для всех пользователей"** (Run whether user is logged on or not).
    *   Отметьте: **"Выполнять с наивысшими правами"** (Run with highest privileges).
4.  **Вкладка "Триггеры":** Создать -> Ежедневно -> Установите время (например, 03:00).
5.  **Вкладка "Действия":** Создать -> Запуск программы.
    *   Программа: `powershell.exe`
    *   Аргументы:
        ```text
        -ExecutionPolicy Bypass -File "C:\Path\to\script.ps1"
        ```
6.  **Вкладка "Условия":**
    *   Уберите галочку: **"Запускать только при питании от электросети"**.
7.  **Вкладка "Параметры":**
    *   Поставьте комфортное время 1ч: **"Останавливать задачу выполняемую дольше"**.

8.  Нажмите ОК и введите пароль пользователя Windows.

---

### 💡 Применение: Бэкап базы 1С

Это идеальный сценарий для файловой базы с организацией сети "Рабочая группа".

**Почему это лучше штатных средств?**
Обычно для бэкапа 1С используют выгрузку в `.dt` (требует ручного запуска или сложных настроек), платный софт или скрипты для принудительного завершения сеансов (которые часто зависают, если кто-то оставил 1С открытой на ночь). Везде есть «подводные камни»: не выгнались пользователи, зависла консоль, не сработал планировщик внутри 1С и т.д. 

**Суть метода:**
Данный скрипт реализует **супер-универсальный способ** для файловых баз.
Вся база данных 1С (файловая версия) физически находится в **одном файле — `1Cv8.1CD`**.
Все остальные файлы в папке базы (лога, блокировок, кэша) — это временные «пристройки». При восстановлении 1С создаст их заново сама.

Поэтому самое надежное решение — просто скопировать (или заархивировать) этот главный файл.

**Пример настройки:**

```powershell
# Указываем путь конкретно к файлу базы (.1CD), а не ко всей папке с мусором
$SourcePath = "C:\Base1C\Accounting\1Cv8.1CD"

# Складываем на отдельный диск
$DestDir = "D:\Backups\1C_Accounting"
$EnableZip = $true   # Включаем сжатие, так как .1CD хорошо сжимаются

# Храним бэкапы за последнюю неделю (7 штук)
$MaxCopies = 7
```

> **Важно:** Желательно, чтобы в момент копирования в базе никто не работал, иначе архив может получиться «битым». Ставьте запуск скрипта на ночь.

---

### ❗ ВАЖНО: Проблема с кодировкой и разрешением выполнения скриптов

По умолчанию политики скрипты нельзя запускать, для того чтобы обойти ограничиение:


*   **Откройте терминал от администратора и введите:** 
```text
Set-ExecutionPolicy RemoteSigned -Scope CurrentUser
```

Если скрипт выдает красные ошибки или пишет в лог "кракозябры" вместо русских букв:

PowerShell чувствителен к кодировке файла, если в путях есть кириллица.
**Файл скрипта должен быть сохранен в кодировке: UTF-8 со спецификацией (UTF-8 with BOM).**

*   **В блокноте:** Файл -> Сохранить как -> Кодировка: **UTF-8 со спецификацией**.
*   **В Notepad++:** Кодировки -> **Преобразовать в UTF-8 BOM**.
*   **В VS Code:** Save with Encoding -> **UTF-8 with BOM**.
