
## 📦Universal PowerShell Backup

Универсальный скрипт для резервного копирования файлов или папок с поддержкой архивации и ротации старых копий.

### Возможности
*   **Режимы:** Простое копирование файлов или упаковка в **ZIP**.
*   **Ротация:** Автоматическое удаление старых бэкапов (хранит последние N копий).
*   **Логирование:** Пишет лог в файл с именем скрипта (например, `backup.ps1` -> `backup.log`) рядом с собой.

### ⚙️ Настройка
Откройте файл `.ps1` в текстовом редакторе и измените блок **НАСТРОЙКИ**:

*   `$SourcePath` — путь к файлу или папке, которую нужно сохранить.
*   `$DestDir` — папка, куда складывать бэкапы.
*   `$EnableZip` — `$true` (архивировать в ZIP) или `$false` (копировать как есть).
*   `$MaxCopies` — количество хранимых копий (старые удаляются).

### ▶️ Ручной запуск
Нажмите правой кнопкой мыши на файл скрипта -> **"Выполнить с помощью PowerShell"** (Run with PowerShell).

---

## 📅 Автозапуск (Планировщик заданий Windows)

Чтобы скрипт выполнялся автоматически по расписанию:

1.  Нажмите `Win + R`, введите **`taskschd.msc`** и нажмите Enter.
2.  Справа выберите **"Создать простую задачу..."** (Create Basic Task).
3.  **Имя:** Введите любое (например, "Test Backup").
4.  **Триггер:** Выберите, как часто запускать (Ежедневно/Еженедельно).
5.  **Действие:** Выберите **"Запустить программу"**.
6.  **Настройка программы:**
    *   **Программа или сценарий:** `powershell.exe`
    *   **Добавить аргументы:**
        ```text
        -ExecutionPolicy Bypass -File "C:\Путь\К\Вашему\Скрипту.ps1"
        ```
        *(Замените путь на реальный путь к вашему файлу)*.
7.  **Финиш:** Нажмите "Готово".

### Важно (если не работает):
Если скрипт должен работать, когда вы не залогинены, или нужны права администратора:
1.  Дважды кликните по созданной задаче в списке.
2.  Вкладка **Общие** (General).
3.  Выберите **"Выполнять для всех пользователей"** (Run whether user is logged on or not).
4.  Поставьте галочку **"Выполнять с наивысшими правами"** (Run with highest privileges).
5.  Нажмите ОК.
